import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return <h1>Gettting started with webpack manually!</h1>;
};

ReactDOM.render(<App />, document.getElementById('root'));